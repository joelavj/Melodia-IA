# Copier ce fichier en `.env` (non versionné, déjà dans .gitignore) et
# adapter les valeurs. connect.py les lit via os.environ.get(...).

MELODIA_DB_USER=root
MELODIA_DB_PASSWORD=
MELODIA_DB_HOST=127.0.0.1
MELODIA_DB_NAME=melodia_ia

# DEBUG, INFO, WARNING ou ERROR
MELODIA_LOG_LEVEL=INFO
